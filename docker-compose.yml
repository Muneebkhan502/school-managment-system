services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER:  "admin"
      POSTGRES_PASSWORD:  "admin1234"
      POSTGRES_DB:  "student_db"
  
  app:
    depends_on:
      db
    image: app-sms
    ports:
      "8000:8000"